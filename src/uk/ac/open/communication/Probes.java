package uk.ac.open.communication;

public class Probes {
/*
	public static double getActualValue(String systemCode, String instanceCode, String apiActualValue, boolean isArray){
		String value = sAPI.getAPIValue(apiActualValue, systemCode, instanceCode);
		if(isArray){
			//tratamento para array
			return 10;
		}else{
			return Double.parseDouble(value);
		}
	}
	
	public static double[] getFuzzySet(String systemCode, String instanceCode, String apiFuzzySet, boolean isArray){
		String value = SimulationAPI.getAPIValue(apiFuzzySet, systemCode, instanceCode);
		if(isArray){
			//tratamento para array
			return new double[]{8,9,10,11,12};
		}else{
			String[] values = value.split(",");
			double[] ret = new double[values.length];
			for (int i = 0; i < values.length; i++) {
				ret[i] = Double.parseDouble(values[i]);
			}
			return ret;
		}
	}*/
}
